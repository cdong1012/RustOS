When we create an Sd object using the function new, we check to make sure sd_init succeed, which means there can only 
have 1 Sd object at a time. Since there is only one access to the Sd object at a time, 
the Sd object and access to sd_err are safe.
